function ejecutar() {
    const nombre = document.getElementById('nombre').value;
    let letra = nombre[0]; 
    consola_salida(letra);
    letra = nombre[1];
    consola_salida(letra);
    letra = nombre[2];
    consola_salida(letra);
    letra = nombre[3];
    consola_salida(letra);
}

