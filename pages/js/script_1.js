function ejecutar() {
    const arreglo = [
        document.getElementById('valor_1').value,
        document.getElementById('valor_2').value,
        document.getElementById('valor_3').value
    ];

    consola_salida(arreglo[0]);
    consola_salida(arreglo[1]);
    consola_salida(arreglo[2]);
}