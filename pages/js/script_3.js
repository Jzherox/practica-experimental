const datos = [];

function ejecutar() {
    datos[0] = document.getElementById('valor_1').value;
    datos[1] = document.getElementById('valor_2').value;
    
    consola_salida(datos);
}

consola_salida(datos);
